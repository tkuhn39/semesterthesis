# Runtime dependencies — installed into the Docker image.
# Keep in sync with semesterthesis_3-12.yaml and the live Conda environment.
# Development/CI tooling lives in requirements-dev.txt.

# --- Web backend ---------------------------------------------------------------
fastapi==0.137.1
uvicorn[standard]==0.49.0
pydantic-settings==2.14.1
python-dotenv==1.2.2

# --- Object storage (only used when STORAGE_BACKEND=s3: AWS S3/R2/Ceph/MinIO) --
boto3==1.43.30

# --- Scientific stack ----------------------------------------------------------
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
matplotlib==3.11.0
gmsh==4.15.2  # FE mesh generator for the gear toothing/rim (model/ service)
